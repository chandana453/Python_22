# this is one fo method to intialise the values into an array
# the thing we have to do is only the parameters we have to pass
# syntax-np.full(the dimensions,the value want to fill)



import numpy as np

a=np.full((3,4,4),3)
print(a)
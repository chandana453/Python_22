# random.rand(),this is also one of the function to initialise the values into the array.
# we have to pass the dimensions only,then the values between the 0 and 1 prints in the float values


import numpy as np
a=np.random.rand(3,3,4)
print(a)
# to initialise some values to the array we have several types.
# in that zeros() is one of the method,all the values fills by seros
# only one thing we have to give the dimensions(nof rows,no of columns,no of subscipts)



import numpy as np

a=np.zeros([2,3,3])
print(a)
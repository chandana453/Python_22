import numpy as np

arr = np.array([3, 2, 0, 1])

print(np.sort(arr))


import numpy as np
arr = np.array(['banana', 'cherry', 'apple'])
print(np.sort(arr))

#Sort a boolean array:

import numpy as np
arr = np.array([True, False, True])
print(np.sort(arr))




#Sort a 2-D array:If you use the sort() method on a 2-D array, both arrays will be sorted

import numpy as np
arr = np.array([[3, 2, 4], [5, 0, 1]])
print(np.sort(arr))